# Backend API (REST + Socket.IO)
REACT_APP_API_URL=http://localhost:3000

# Google OAuth (optional — also set in App.jsx if not using env)
# REACT_APP_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
